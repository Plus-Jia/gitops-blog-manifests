# gitops-blog-manifests

Kubernetes and ArgoCD deployment configuration.

- `apps/blog`: static Astro frontend deployment
- `apps/admin`: content admin deployment
- `argocd`: ArgoCD Applications

Blog and admin are intentionally deployed as separate Deployments.
